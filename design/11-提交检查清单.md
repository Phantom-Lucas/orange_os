# 提交与答辩检查清单

当前日期为 2026-08-21，课程通知要求在 2026-08-31 前完成线上答辩。单人组的本人同时是
组长。以下各项必须由本人逐项确认。

## 1. 本人信息与合规

- [ ] `design/` 中 `[姓名]`、`[学号]`、`[班级]`、`[组号]` 已全部替换。
- [ ] 已与老师确认单人组和 50% 代码量/AI 辅助的认定口径。
- [ ] [成员分工与原创性说明](07-成员分工与原创性说明.md)逐模块填写完成。
- [ ] 所有参考代码、文章、字体和 AI 使用均如实列出。
- [ ] 能脱稿解释 Loader、COW、FS、键盘/TTY 四条主线中的至少三条。
- [ ] 未出现“100% 原创”“全部手写”等无法证明的表述。

## 2. 源码冻结

- [ ] 将功能分支变更审查后合入主分支。
- [ ] `git status --short` 为空，或所有保留文件均明确纳入提交。
- [ ] `git diff --check` 无空白错误。
- [ ] 无磁盘镜像、测试 artifact、个人路径、密钥或大体积临时文件误提交。
- [ ] 最终提交有清晰 commit message。
- [ ] 创建不可歧义的 tag，例如 `os-course-design-2026-defense`。
- [ ] GitHub 链接固定到最终 tag/commit，不只给可能继续变化的仓库首页。
- [ ] 在另一目录从该链接克隆并成功构建一次。

建议冻结命令（tag 名可调整）：

```bash
git status --short --branch
git diff --check
git log -1 --oneline
git tag os-course-design-2026-defense
```

是否 commit、merge、push、tag 由本人检查后执行；本文档不会自动修改远程仓库。

## 3. 最终验证

- [ ] `make check` PASS。
- [ ] `make test-self` PASS。
- [ ] `make test PROFILE=full SEED=20260821 KEEP_FAILED=1` 为 16/16 PASS。
- [ ] `make test CASE=sync.stress SEED=20260821 KEEP_FAILED=1` PASS，或报告保留同版本最近结果。
- [ ] 最终 commit/tag、测试日期、artifact 路径写入测试报告。
- [ ] QEMU 中手工执行 `about`、`ps`、`demo`、`cat demo-proof.txt` 均正确。
- [ ] QEMU 退出/重启后 proof 文件仍存在。
- [ ] framebuffer 和 VGA 回退各启动一次。

## 4. 文档定稿

- [ ] 从 [README](README.md) 顺序完整阅读，删除互相矛盾的旧数字和未来时表述。
- [ ] 项目报告中的制品尺寸用最终 tag 重新确认。
- [ ] 测试报告不把历史失败删掉，也不把 infrastructure/timeout 改写成 PASS。
- [ ] Mermaid 图已在导出工具中正常渲染；若 Word 不支持，导出为 PNG 再插入。
- [ ] 两张真实运行截图放入 `design/assets/`，无个人隐私、无伪造内容。
- [ ] 所有相对链接在最终 PDF/Word 中转成可访问的章节或附录。
- [ ] 最终阅读导航导出为 Word 或 PDF；源码只提供托管链接，不附源码压缩包。

建议最终文档合并顺序：

```text
封面
诚信/原创性说明
摘要
目录（可点击）
项目设计与实现报告
Orange'S 对照与代码审核
功能与演示证据
环境搭建与复现
测试报告
问题解决与心得
成员分工与来源
答辩附录/问题库
```

## 5. PPT 与演示

- [ ] 12 页主线已按 [PPT 逐页设计](09-PPT逐页设计.md)制作。
- [ ] P11 的个人贡献与 AI 使用换成真实内容。
- [ ] 准备 syscall、MyFS、syscall entry、完整测试四张隐藏备份页。
- [ ] 演示镜像用 `make fb-showcase-prepare` 准备一次，此后不再格式化。
- [ ] 演示当天 QEMU 预先停在 prompt，网络断开也可完成。
- [ ] 完成三次彩排，稳定在 9:20～9:40，留出意外缓冲。
- [ ] `demo` 失败、QEMU 卡死、投影太小三种预案都练过。
- [ ] 不在现场跑 full/stress，不在现场 merge/pull/build。

## 6. 邮件与压缩包

按课程通知，预答辩材料至少提前 24 小时发送到 `wang.box@163.com`，并通过 QQ 私信王老师
确认。提交顺序影响答辩批次，越早完成可能有加分。

- [ ] 邮件标题：`[组号]组[组长姓名]操作系统课程设计答辩`
- [ ] 压缩包名：`[组号]组组[组长姓名]操作系统课程设计`
- [ ] 压缩包包含可导航项目文档（Word/PDF）。
- [ ] 压缩包包含源码托管链接文本，不包含源码包。
- [ ] 压缩包包含答辩 PPT（虽为可选，实际答辩需要）。
- [ ] 压缩包不包含 `build/`、磁盘镜像、Git 历史、缓存和无关资料。
- [ ] 邮件发出后核对附件可打开，并 QQ 确认老师收到。

## 7. 建议交付结构

```text
[组号]组组[姓名]操作系统课程设计/
├── Orange64-项目文档.pdf
├── Orange64-答辩PPT.pptx
├── 源码托管链接.txt
└── 演示与复现说明.pdf        # 可并入项目文档
```

## 8. 最终签字

```text
最终 commit：________________________________
最终 tag：___________________________________
full artifact：______________________________
stress artifact：____________________________
文档校对日期：_______________________________
邮件发送时间：_______________________________
QQ 确认时间：________________________________
本人签名：___________________________________
```

